insert into
    configurations (configurationName, configurationLabel, configurationValue, color, weight)
values
    (
        "status", "taskUI.status-create", "CREATE", "#56F356FF", 100
    )
;



insert into
    configurations (configurationName, configurationLabel, configurationValue, color, weight)
values
    (
        "status", "taskUI.status-todo", "TODO", "#E1D5D5FF", 200
    )
;



insert into
    configurations (configurationName, configurationLabel, configurationValue, color, weight)
values
    (
        "status", "taskUI.status-in-progress", "IN_PROGRESS", "#EA6A0FFF", 300
    )
;



insert into
    configurations (configurationName, configurationLabel, configurationValue, color, weight)
values
    (
        "status", "taskUI.status-waiting-for-review", "WAITING_FOR_REVIEW", "#FF003EFF", 400
    )
;



insert into
    configurations (configurationName, configurationLabel, configurationValue, color, weight)
values
    (
        "status", "taskUI.task-status-in-review", "IN_REVIEW", "#FFD500FF", 500
    )
;



insert into
    configurations (configurationName, configurationLabel, configurationValue,color, weight)
values
    (
        "status", "taskUI.status-waiting-for-test", "WAITING_FOR_TEST", "#C4DA04FF", 600
    )
;



insert into
    configurations (configurationName, configurationLabel, configurationValue, color, weight)
values
    (
        "status", "taskUI.task-status-test", "TEST", "#04C8DAFF", 700
    )
;

insert into
    configurations (configurationName, configurationLabel, configurationValue, color, weight)
values
    (
        "priority", "taskUI.priority-trivial", "TRIVIAL", "#56f356", 100
    )
;

insert into
    configurations (configurationName, configurationLabel, configurationValue, color, weight)
values
    (
        "priority", "taskUI.priority-minor", "MINOR", "#0E6E03FF", 200
    )
;

insert into
    configurations (configurationName, configurationLabel, configurationValue, color, weight)
values
    (
        "priority", "taskUI.priority-major", "MAJOR", "#F2FC07FF", 300
    )
;

insert into
    configurations (configurationName, configurationLabel, configurationValue, color, weight)
values
    (
        "priority", "taskUI.priority-critical", "CRITICAL", "#FC0707FF", 400
    )
;

