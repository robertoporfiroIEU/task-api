spring:
  h2:
    console:
      enabled: true
  datasource:
    url: "jdbc:h2:mem:tasksAPIDB"
    username: sa
    password: password
  jpa:
    database-platform: "org.hibernate.dialect.H2Dialect"